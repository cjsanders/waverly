services:
  affiliate:
    command: >-
      WORKOS_REDIRECT_URI="$PUBLIC_URL"api/auth/callback
      CLOUDFLARE_ENV="${CLOUDFLARE_ENV:-dev}"
      bunx vite dev --host 0.0.0.0 --port "$PORT"
    cwd: apps/affiliate
    health: /
    portal:
      url: /
      title: Affiliate app
    review: true
    agent: control
