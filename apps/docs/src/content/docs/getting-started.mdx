---
title: Getting started
description: Build and operate the Waverly affiliate network.
---

Start here for product, integration, and operational documentation.

Cloud Agents and local tools sign in through the [agent login](/agent-login) route when `TEST_USER_EMAIL` and `TEST_USER_PASSWORD` are set. That path is dev-only and never ships in production.
